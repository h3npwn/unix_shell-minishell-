# unix_shell-minishell-
minishell` is a small, functional Unix shell written in C.   It reproduces the basic behavior of **bash**, including command parsing, environment handling, and process execution — giving a deeper understanding of how a real shell works behind the scenes.
